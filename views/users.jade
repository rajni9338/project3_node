extends layout

block content
  h1 Login
  -if (message){
       .alert.messageClass #{message}   
    -} 
  form(name="loginForm", method="POST", action="/users")
        div.input4
            span.label Email: 
            br
            input(type="email", id="email", name="email")
        br
        div.input4
            span.label Password: 
            br
            input(type="password", id="password", name="password")
        div.actions
            input(type="submit", id="register", value="login")
  
  br
  button(type="submit" onclick="window.location.href = '/register';") Register